numero=int(input("introduzca un numero" ))
factor = 1
g = 1
if (numero > 0) : 
    while factor<=numero :
     factor=factor * g
     g = g + 1
     print=(str(factor))
     break
else:
   print("ingrese un factor real o mayor a 0");